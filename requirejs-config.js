var config = {
    paths: {            
            fontawesome: "js/fontawesome",
            bootstrapjs: "js/bootstrap.min",
        },   
    shim: {
        'fontawesome': {
            deps: ['jquery']
        }
    }
};